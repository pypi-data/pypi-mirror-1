from kss.core import KSSView, kssaction
import datetime

class KSSDemoView(KSSView):

    # Create the server actions needed for your demo.
    # Use the following method as example.
    @kssaction
    def myAction(self, value):
        """my demo action"""
        # XXX replace this with what you need to do
        self.getCommandSet('core').replaceInnerHTML('div#exampleId', 
            'click me again! (value="%s" at %s)' % (value, datetime.datetime.now()))
