

#>>> for feed in atomfeeds:  
#...     print 'accessing feed:', feed
#...     response = self.publish('/plone/Members/test_user_1/' + feed)
#...     validator = setup.validateFeed(response.getBody(), feed, 'utf-8',
#...                                    ['http://localhost/' + feed])
#...     events = validator.loggedEvents
#...     from feedvalidator import compatibility
#...     events = compatibility.AA(events)
#...     from feedvalidator.formatter.text_plain import Formatter
#...     output = Formatter(events)
#...     if output:
#...         print response.getBody()
#... #    parsed = parse(response.getBody())
#... #    setup.compare('atom.xml', parsed, self.folder)
