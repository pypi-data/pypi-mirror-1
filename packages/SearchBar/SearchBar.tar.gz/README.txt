Installation:

* Go to IDLE's directory: <python-dir>/Lib/idleib/
* Edit config-extensions.def, and add the text in add_to_config_extensions.def
  (replace the existing section if it already exists)
* Copy all other files in the archive (i.e. not README.txt and add_to_config_extensions.def)
