<%
    gzip = config['global']['gzip']
%>\
order allow,deny
allow from all

<Files ~ "^\.htaccess$">
  deny from all
</Files>

%if gzip:
<IfModule mod_headers.c>
  Header set Content-Encoding  "gzip"
</IfModule>
%endif