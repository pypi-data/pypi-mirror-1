<% header(name + ' ' + versionString + ' Release Notes') %>

<p><% name %> version <% versionString %> released on <% releaseDate %></p>

<a name="Improvements"></a><h2>Improvements and Refinements</h2>
<ul>
	<li>Ensured backward compatibility with older Python versions.</li>
	<li>Made <code>store.object()</code> more flexible: it can take an
	<code>ObjectKey</code> or a separate class and serial num.</li>
	<li>Removed the timestamp from the Python code generator to reduce
    source code control diffs.
    There is still a timestamp in <span class="filename">Info.text</span>.</li>
</ul>

<% footer() %>