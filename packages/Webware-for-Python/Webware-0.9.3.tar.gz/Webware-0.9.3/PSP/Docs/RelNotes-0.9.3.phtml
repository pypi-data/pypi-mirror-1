<% header(name + ' ' + versionString + ' Release Notes') %>

<p><% name %> version <% versionString %> released on <% releaseDate %></p>

<a name="Bugfixes"></a><h2>Bugfixes</h2>
<ul>
	<li>A bug has been fixed that prevented using <code>psp-handler</code> with
	PSP pages outside any Webware context (bug report by Fionn Behrens).</li>
</ul>

<% footer() %>