#!/bin/bash
#
# it's very handy when every commit leave a log file locally
# so this script does that by overwriting cvs ci with own version
#
# Author: Valentin Kuznetsov, 2008

msg=$1; shift

date=`date`
cat >> version.info << EOF
$date -- $msg
EOF

cvs ci -m "$msg" $@ version.info

