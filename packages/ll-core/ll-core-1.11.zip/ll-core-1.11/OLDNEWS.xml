<p>This following changes were done before those modules were merged into
one distribution package.</p>

<section><h>Changes in the old core package</h>

<section><h>Changes in ll-core 0.3 (released 05/24/2005)</h>
<ul>
<li>Functions will now no longer be turned into
<func>staticmethod</func>s automatically when used in a
<class>Namespace</class>.</li>
<li>The iterator tools from <mod>ll.xist.xfind</mod>
(<func>item</func>, <func>first</func>, <func>last</func>,
<func>count</func> and <class>Iterator</class>) have been move here,
as they are in no way specific to &xist;.</li>
<li>A test suite has been added.</li>
<li>The wrapper function returned by <func>notimplemented</func>
will now have an attribute <lit>__wrapped__</lit> that points back to the
original function.</li>
</ul>
</section>


<section><h>Changes in ll-core 0.2.1 (released 01/21/2005)</h>
<ul>
<li><meth>__getitem__</meth> now raises a KeyError if the
attribute doesn't exist.</li>
</ul>
</section>


<section><h>Changes in ll-core 0.2 (released 01/11/2005)</h>
<ul>
<li><class>Namespace</class> now has a <meth>__getitem__</meth>
method, so a <class>Namespace</class> class can be used in a
call to the <func>eval</func> function.</li>
</ul>
</section>


<section><h>Changes in ll-core 0.1 (released 01/03/2005)</h>
<ul>
<li>Initial release</li>
</ul>
</section>

</section>


<section><h>Changes in ll-ansistyle</h>

<section><h>Changes in ll-ansistyle 1.1 (released 06/28/2005)</h>
<ul>
<li>The methods <meth>pushcolor</meth> and <meth>popcolor</meth>
have been resurrected. Without them switching to a new color and back would
have to be done in a single call to <meth>feed</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 1.0 (released 06/08/2005)</h>
<ul>
<li><mod>ll.ansistyle</mod> has been completely reimplemented to use
an iterator interface instead of a stream interface.</li>
<li>Support for underlined and blinking text has been added.</li>
<li>A <a href="http://codespeak.net/py/current/doc/test.html">py.test</a>
based test suite has been added.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.6.1 (released 03/22/2005)</h>
<ul>
<li>Added a note about the package init file to the installation
documentation.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.6 (released 01/03/2005)</h>
<ul>
<li><mod>ansistyle</mod> requires the core module and
Python 2.4 now.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.5 (released 05/21/2004)</h>
<ul>
<li><class>Text</class> has been derived from <class>list</class>
directly, so it inherits all list methods.</li>
<li>The method <meth>getcolor</meth> has been dropped.
The class attribute <lit>color</lit> is used now instead.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.4 (released 07/31/2003)</h>
<ul>
<li>The names of the methods <meth>pushColor</meth>,
<meth>popColor</meth>, <meth>getColor</meth> and
<meth>escapeChar</meth> have been changed to lowercase.</li>
<li><mod>ansistyle</mod> requires Python 2.3 now.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.3.1 (released 11/14/2002)</h>
<ul>
<li>Added source code encodings to all Python files.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.3 (released 08/27/2002)</h>
<ul>
<li><mod>ansistyle</mod> has been moved to the
<mod>ll</mod> package.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.2.2 (released 02/12/2002)</h>
<ul>
<li>Fixed a bug in <meth>Text.insert</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.2.1 (released 04/11/2001)</h>
<ul>
<li>ansistyle now compiles under Windows with Visual C++.
A binary distribution archive is available from the
&ftp; directory.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.2 (released 04/02/2001)</h>
<ul>
<li>ansistyle now supports background colors. You can specify
the background color via the bits 4-7 of the color, i.e.
for the background color b = 0,...,7, and the foreground
color f=0,...,15 the color value is <lit>(b&lt;&lt;4)|f</lit>.</li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.1.1 (released 03/21/2001)</h>
<ul>
<li>Fixed a minor bug in <meth>ansistyle.Text.__repr__</meth></li>
</ul>
</section>


<section><h>Changes in ll-ansistyle 0.1 (released 02/18/2001)</h>
<ul>
<li>Initial release</li>
</ul>
</section>

</section>


<section><h>Changes in ll-color</h>

<section><h>Changes in ll-color 0.3.1 (released 03/22/2005)</h>
<ul>
<li>Added a note about the package init file to the installation
documentation.</li>
</ul>
</section>


<section><h>Changes in ll-color 0.3 (released 01/21/2005)</h>
<ul>
<li>Two new methods (<meth>abslum</meth> and <meth>rellum</meth>)
have been added that return a color with modified luminosity.</li>
</ul>
</section>


<section><h>Changes in ll-color 0.2 (released 01/03/2005)</h>
<ul>
<li><mod>color</mod> requires the core module and Python 2.4 now.</li>
<li>Various bug fixes.</li>
</ul>
</section>


<section><h>Changes in ll-color 0.1.1 (released 05/07/2004)</h>
<ul>
<li>Fixed a bug in the <prop>css</prop> property.</li>
</ul>
</section>


<section><h>Changes in ll-color 0.1 (released 05/07/2004)</h>
<ul>
<li>Initial release.</li>
</ul>
</section>

</section>


<section><h>Changes in ll-make</h>

<section><h>Changes in ll-make 1.1.2 (released 10/04/2005)</h>
<ul>
<li>Fixed a bug in the handling of color environment variables.</li>
</ul>
</section>


<section><h>Changes in ll-make 1.1.1 (released 09/02/2005)</h>
<ul>
<li>Specifying colors via environment variables now works.</li>
<li>It's possible to specify a default for the <lit>show<rep>...</rep></lit>
options via environment variables.</li>
<li><class>CacheAction</class> now drops the data in its <meth>clear</meth>
method.</li>
</ul>
</section>


<section><h>Changes in ll-make 1.1 (released 09/01/2005)</h>
<ul>
<li>New action classes have been added: <class>PickleAction</class>,
<class>UnpickleAction</class>, <class>NullAction</class> and
<class>CacheAction</class>.</li>
<li>During calls to <meth>Target.clear</meth> and <meth>Target.dirty</meth>
the action methods with the same name are now called.</li>
</ul>
</section>


<section><h>Changes in ll-make 1.0 (released 08/29/2005)</h>
<ul>
<li><class>Target</class>s may now cache the objects that they create, so
it can be reused for different outputs.</li>
<li>The <class>Action</class> chain has been split into four chains that will
be used in different situations. Each target has an internal and external
representation (e.g. the Python <lit>str</lit> object (the internal representation),
that is the content of a file (the external representation). The read chain
creates the internal representation from the external one, the write chain
creates the external representation from the internal one. The convert chain
converts between different internal representations. The use chain is called
when external and internal representation exist and are up to date.</li>
<li>The internal representation of a target is now available via the
method <meth>getdata</meth>.</li>
<li>Importing Python modules is now done via an <class>ImportAction</class>.</li>
<li><class>ImportAction</class> and <class>UseModuleAction</class> can be
used to automatically track module dependencies.</li>
<li>During build operations the currently <z>running</z> project is available
as <lit>ll.make.currentproject</lit>.</li>
<li>Two new action classes are available: <class>SelectMainAction</class>
and <class>JoinOrderedAction</class>, which can be used to select the input
data at the start of a convert chain.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.26 (released 05/29/2005)</h>
<ul>
<li>Uses <mod>ansistyle</mod> 1.1 now.</li>
<li>Introduced a new <class>Action</class> class named <class>ChainedAction</class>
that consists of a list of other actions. Each <class>Target</class> now only
has one action to update this target, but this action might be a
<class>ChainedAction</class>. <class>Action</class>s can be added (which
results in a <class>ChainedAction</class>).</li>
</ul>
</section>


<section><h>Changes in ll-make 0.25 (released 05/20/2005)</h>
<ul>
<li>make is compatible with &xist; 2.10 now.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.24 (released 04/11/2005)</h>
<ul>
<li><class>XPITAction</class> now works if there is no namespace
available. In this case only the global namespace will be passed
to the expressions.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.23.1 (released 03/22/2005)</h>
<ul>
<li>Added a note about the package init file to the installation
documentation.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.23 (released 02/14/2005)</h>
<ul>
<li>Actions can now be displayed during the make process in two ways:
a short name (this uses the method <meth>desc</meth>) and
a longer description (using the method <meth>fulldesc</meth>).
You can activate the full description via the command line option
<option>-vf</option> and deactivate it with <option>-vF</option>.
In interactive mode you can use the attribute <lit>showactionsfull</lit>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.22 (released 01/21/2005)</h>
<ul>
<li><class>XPITAction</class> will now pass the project, target
and action to the embedded Python expression as global variables.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.21 (released 01/19/2005)</h>
<ul>
<li>An action class <class>XPITAction</class> has been added for use
with <mod>ll.xpit</mod>.</li>
<li>Setting a project dirty (so that out-of-dateness will be rechecked)
has been factored into a separate method.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.20 (released 01/03/2005)</h>
<ul>
<li><mod>make</mod> requires the core module and Python 2.4 now.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.19.1 (released 11/26/2004)</h>
<ul>
<li>Fixed print of tracebacks when <lit>ignoreerrors</lit> is true.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.19 (released 10/29/2004)</h>
<ul>
<li><mod>ll.make</mod> is compatible with &xist; 2.6 now
(and incompatible with &xist; 2.5).</li>
</ul>
</section>


<section><h>Changes in ll-make 0.18.2 (released 10/12/2004)</h>
<ul>
<li>Retry with absolute and real &url;s in <meth>__candidates</meth>
even if the argument is already an <class>ll.url.URL</class> object. This
works around an URL normalization bug under Windows.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.18.1 (released 08/27/2004)</h>
<ul>
<li><lit>Target.actions</lit> is now a list instead of a tuple.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.18 (released 07/06/2004)</h>
<ul>
<li>Added a new action class <class>TOXICPrettifyAction</class> that uses the
new <func>prettify</func> function introduced in <mod>ll.toxic</mod>
version 0.3.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.17 (released 06/02/2004)</h>
<ul>
<li>Renamed <class>OracleTarget</class> to <class>DBTarget</class>.</li>
<li>Reporting <class>PhonyTarget</class>s has been moved to a separate
method named <meth>reportphonytargets</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.16 (released 05/31/2004)</h>
<ul>
<li>The method <meth>buildWithArgs</meth> has been dropped.
Use <meth>buildwithargs</meth> now.</li>
<li>Argument parsing has been made extensible. The method <meth>optionparser</meth>
must return an instance of <class>optparse.OptionParser</class>. The method
<meth>parseoptions</meth> parses the argument sequence passed in
(defaults to <lit>sys.argv[1:]</lit> and returns a tuple with <lit>(<rep>values</rep>,
<rep>args</rep>)</lit> (just like <meth>optparse.OptionParser.parse_args</meth> does).</li>
<li>The arguments <arg>ignoreerrors</arg>, <arg>color</arg>, <arg>maxinputreport</arg>
have been removed from the <class>Project</class> constructor. If you really need
different values for these, simply change the attributes after creating the
<class>Project</class> object.</li>
<li><meth>Project.__getitem__</meth> and <meth>Project.__contains__</meth>
now recognize database ids.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.15.1 (released 05/25/2004)</h>
<ul>
<li>Fixed formatting bugs in <class>OracleReadResource</class>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.15 (released 05/25/2004)</h>
<ul>
<li>There's a new option <option>-vl</option> that reports the recursion
level as an indentation during the build process. This makes it easier to
see, what depends on what. The indentation per level can be specified with
the environment variable <lit>LL_MAKE_INDENT</lit>.</li>
<li>The environment variable <lit>MAKE_REPRANSI</lit> has been renamed to
<lit>LL_MAKE_REPRANSI</lit>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.14.2 (released 05/25/2004)</h>
<ul>
<li>If a target has prerequisites, the time to rebuild those
will be reported in the progress report too (if time reporting is on (via the
option <option>-vt</option>)).</li>
<li>Fix a bug in <class>XISTPublishAction</class>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.14.1 (released 05/21/2004)</h>
<ul>
<li>The default color for output has been removed.</li>
<li>In the progress report &url;s relative to the home directory
are now tried too to find the shortest &url; for display.</li>
<li>Fix a bug in <class>JoinedReadAction</class> and various
other bugs.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.14 (released 05/20/2004)</h>
<ul>
<li><p>Actions have been made much more atomic and flexible. For each
target a chain of actions will be executed. The first action loads the
file. The next actions transform the content, after that an action
will save the result to a file. Finally other actions can modify this
file (what has formerly been known as <z>secondary actions</z>).</p>
<p>For example: to transform an &xist; file now you need a <class>ReadAction</class>,
a <class>XISTParseAction</class>, a <class>XISTConvertAction</class>, a
<class>XISTPublishAction</class> and a <class>WriteAction</class>. The
base <class>URL</class>s for parsing and publishing have been moved
from <class>XISTTarget</class> to <class>XISTParseAction</class> and
<class>XISTPublishAction</class>.</p></li>
<li><class>DBID</class> has been rewritten. For Oracle <class>DBID</class>s
it's possible to read and write functions and procedures via a file-like
interface.</li>
<li>Support for <a href="http://xml.apache.org/fop/index.html">Apache FOP</a>
and <a href="root:toxic/index.html">ll-toxic</a> has been added.</li>
<li>The <class>Target</class> methods <meth>sources</meth> and <meth>targets</meth>
have been renamed to <meth>inputs</meth> and <meth>outputs</meth> (related
methods have been renamed too).</li>
<li>The options for selecting the verbosity of the progress report have
been combined into one option <option>-v</option>.</li>
<li>The progress report tries to shorten <class>URL</class>s by displaying
relative <class>URL</class>s (relative to the current directory) if those are shorter
(which they usually are).</li>
</ul>
</section>


<section><h>Changes in ll-make 0.13.1 (released 05/05/2004)</h>
<ul>
<li>Fixed a small bug in <meth>Project.__contains__</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.13 (released 01/12/2004)</h>
<ul>
<li>Now after the build the import cache <lit>ll.url.importcache</lit>
will be restored to the state before the call. This fixes a bug, where a module
that was loaded from another module (not as a <class>PythonTarget</class>),
didn't get cleared from the import cache.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.12 (released 01/02/2004)</h>
<ul>
<li>Adapted to &xist; 2.4. <class>XISTTarget</class> now has two
attributes <lit>parser</lit> and <lit>publisher</lit> which will be used
by <class>XISTAction</class> for parsing and publishing targets.</li>
<li>Changed the assertions that check that <class>XISTAction</class>,
<class>CopyAction</class> and <class>SplatAction</class> have only one
source into exceptions.</li>
<li><meth>Project.__getitem__</meth> and related methods will
now only try absolute file paths, if the &url; really is local.</li>
<li>Dropped the deprecated project method <meth>has</meth>.</li>
<li>For parsing the command line option <mod>optparse</mod> is used
now instead of <mod>getopt</mod>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11.7 (released 12/15/2003)</h>
<ul>
<li>When building a target fails, the file will now only be removed if it
exists.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11.6 (released 12/08/2003)</h>
<ul>
<li>Remove the module from the import cache in <meth>PythonTarget.clear</meth>,
so that the module will be reloaded when <meth>Project.recreate</meth> is used.</li>
<li>Made compatible with &xist; 2.3.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11.5 (released 12/06/2003)</h>
<ul>
<li>Now when a project is rebuilt, all loaded Python modules
will be removed from the import cache before rebuilding commences. This
should fix intermodule dependencies.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11.4 (released 12/06/2003)</h>
<ul>
<li>Added methods <meth>itersources</meth>, <meth>itertargets</meth>,
<meth>itersourcedeps</meth> and <meth>itertargetdeps</meth> to the
<class>Target</class> class.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11.3 (released 11/22/2003)</h>
<ul>
<li><meth>__getitem__</meth> and <meth>__contains__</meth>
of the <class>Project</class> class now first try with an
absolute filename and then with the real filename (i.e. all links
resolved).</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11.2 (released 08/06/2003)</h>
<ul>
<li>A few of the <class>Project</class> attributes have been
renamed to avoid name clashes when a class was derived from
<class>Project</class> and <class>ll.sisyphus.Job</class>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11.1 (released 08/01/2003)</h>
<ul>
<li>Fixed a bug in <meth>Project.build</meth>: Timestamps
were not cleared on the second call to <meth>build</meth>
when the first one had failed.</li>
<li>Timestamp handling was broken. Timestamps from the filesystem
were in &utc;, but the timestamp set after calls to <meth>Target.update</meth>
were in local time. This has been fixed now.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.11 (released 07/31/2003)</h>
<ul>
<li>Calling the &xist; conversion in <class>XISTAction</class> has been
moved from <meth>execute</meth> to a new method <meth>convert</meth>
to be easier to customize.</li>
<li><mod>make</mod> requires Python 2.3 now.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.10 (released 07/02/2003)</h>
<ul>
<li>Targets will now be removed when building them fails.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.9.5 (released 05/02/2003)</h>
<ul>
<li><meth>Project.__getitem__</meth> now retries with a
canonical filename (i.e. the result of the <meth>real</meth>)
before giving up.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.9.4 (released 04/24/2003)</h>
<ul>
<li>All primary actions now make sure that the output file
is removed when an error happens. The next call to a make
script will again try to generate the output instead of silently
skipping the half finished (but seemingly up to date) file.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.9.3 (released 04/23/2003)</h>
<ul>
<li>Use the enhanced <meth>import_</meth> method from <mod>ll.url</mod> 0.7.</li>
<li>Add a <lit>doc</lit> attribute to <class>PhonyTarget</class> which can
be used in help messages (e.g. when <meth>buildWithArgs</meth> is called
without arguments).</li>
</ul>
</section>


<section><h>Changes in ll-make 0.9.2 (released 04/15/2003)</h>
<ul>
<li>Fixed a small bug in the deprecated <meth>Project.has</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.9.1 (released 03/11/2003)</h>
<ul>
<li>Fixed a small bug in <meth>Target.lastmodified</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.9 (released 03/10/2003)</h>
<ul>
<li>Generating a <class>Publisher</class> in an <class>XISTAction</class>
has been moved to a separate method <meth>publisher</meth>.</li>
<li>Each target can now be assigned a sequence of actions. There
are new action classes <class>ModeAction</class> and <class>OwnerAction</class>
that change the access permissions or owner of a file that has been
created by a previous action in an action sequence.</li>
<li>Updated the timestamp functionality so that with Python 2.3
the <mod>datetime</mod> module will be used for timestamps.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.8 (released 03/03/2003)</h>
<ul>
<li>The project method <meth>has</meth> has been deprecated.
Use <meth>has_key</meth> or the new <meth>__contains__</meth>
for that. This means that all dictionary access method now, try
strings, &url;s and absolute &url;s now.</li>
<li>Populating a project can now be done in the overwritable
method <meth>create</meth>. There is a new method <meth>clear</meth>
which removes all targets from the project. Use the method
<meth>recreate</meth> to recreate a project, i.e. call
<meth>clear</meth> and <meth>create</meth>.
</li>
</ul>
</section>


<section><h>Changes in ll-make 0.7 (released 02/26/2003)</h>
<ul>
<li>Made compatible with &xist; 1.5 again: <lit>prefixes</lit>
is only passed to the parser, when it is not <lit>None</lit>.</li>
<li><meth>has</meth> and <meth>has_key</meth> have been changed
to do the same as <meth>__getitem__</meth>, i.e. retry with an &url; or
absolute &url; in case of an error.</li>
<li><meth>build</meth> can now be called multiple times and will
reset timestamp information on all subsequent calls. This makes it possible
to rerun a build process without having to recreate the project with its targets
and dependencies (provided that no targets have to be added or removed).</li>
</ul>
</section>


<section><h>Changes in ll-make 0.6.1 (released 02/14/2003)</h>
<ul>
<li><class>XISTTarget</class> has new attributes <lit>parser</lit>,
<lit>handler</lit> and <lit>prefixes</lit> that can be specified
in the constructor and will be used for parsing.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.6 (released 11/20/2002)</h>
<ul>
<li><meth>Project.__getitem__</meth> now raises an <class>UndefinedTargetError</class>
exception with the original key if retrying with <class>URL</class>s fails.</li>
<li>The methods <meth>Target.sources</meth> and <meth>Target.targets</meth>
have been changed to return the <class>Target</class> objects instead of the
<class>Dep</class> objects. The old functionality is still available
as <meth>Target.sourcedeps</meth> and <meth>Target.targetdeps</meth>. The
same has been done for the method <meth>Target.newerSources</meth> (and
the method name has been made lowercase).</li>
</ul>
</section>


<section><h>Changes in ll-make 0.5 (released 11/13/2002)</h>
<ul>
<li><class>Project</class> is derived from <class>dict</class> now.</li>
<li>Calling <meth>Project.buildWithArgs</meth> with
an empty argument list now lists all <class>PhonyTarget</class>s.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.4.2 (released 11/11/2002)</h>
<ul>
<li>Added a new target class <class>JavaPropAction</class>,
for Java property files.</li>
<li>Added a <meth>__len__</meth> to the <class>Project</class>
class.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.4.1 (released 10/25/2002)</h>
<ul>
<li>Added a new action class <class>SplatAction</class>,
that can be used for replacing strings in files.</li>
<li>Speed up dependency creation by adding slot
declarations.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.4 (released 08/27/2002)</h>
<ul>
<li>Adapted to &xist; 2.0.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.3.2 (released 06/16/2002)</h>
<ul>
<li>Work around a problem with unicode objects in <lit>sys.path</lit>.
This workaround will disappear as soon as Python 2.3 is released.</li>
<li>Use the method <meth>doPublication</meth> for publishing
nodes. (This requires &xist; 1.4.4.)</li>
</ul>
</section>


<section><h>Changes in ll-make 0.3.1 (released 03/28/2002)</h>
<ul>
<li>Added a warning when the id of a new target already exists in the
project, i.e. when the target is redefined.</li>
<li>Added a warning for file modification timestamps from the future.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.3 (released 03/18/2002)</h>
<ul>
<li>Now <class>url.URL</class> is used everywhere instead of
<class>fileutils.Filename</class>
</li>
</ul>
</section>


<section><h>Changes in ll-make 0.2.3 (released 02/22/2002)</h>
<ul>
<li>Added a new class <class>DBID</class> that can be used as an
id for database content.</li>
<li>Ported to Python 2.2</li>
</ul>
</section>


<section><h>Changes in ll-make 0.2.2 (released 01/25/2001)</h>
<ul>
<li>Verbosity can now be specified via several <class>Project</class>
constructor arguments.</li>
<li><meth>Action.converter</meth> now sets the attribute
<lit>makeaction</lit> on the returned <class>Converter</class> object.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.2.1 (released 10/03/2001)</h>
<ul>
<li>Support for the <arg>root</arg> paramenter for the
<meth>convert</meth> method in <class>XISTAction</class>.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.2 (released 10/02/2001)</h>
<ul>
<li>Dependencies now have a type (a subclass of <class>Dep</class>).
This allows to mark certain dependencies as <z>special</z>.</li>
<li><meth>Project.build</meth> can now be called with a <class>Target</class>
or a filename as a string.</li>
</ul>
</section>


<section><h>Changes in ll-make 0.1 (released 07/27/2001)</h>
<ul>
<li>Initial release.</li>
</ul>
</section>

</section>


<section><h>Changes in ll-sisyphus</h>

<section><h>Changes in ll-sisyphus 0.10.1 (released 03/22/2005)</h>
<ul>
<li>Added a note about the package init file to the installation
documentation.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.10 (released 01/03/2005)</h>
<ul>
<li><mod>sisyphus</mod> requires the core module and Python 2.4 now.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.9.1 (released 04/28/2004)</h>
<ul>
<li>Fixed a bug related to logging empty strings.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.9 (released 11/13/2003)</h>
<ul>
<li>Lowercased the constructor arguments <arg>maxRuntime</arg>,
<arg>raiseErrors</arg> and <arg>printKills</arg>.</li>
<li>When the job is started it checks whether it's predecessor is
still running (i.e. it checks whether the pid from the run file
really exists).</li>
<li>Added a method <meth>logErrorOnly</meth> that writes
to the error log only (this is used when the message about a
job still running is written to the error log, so the progress
log from the previous job execution won't be disturbed).</li>
<li>The loop log now contains the exception value in case
of an error.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.8 (released 07/31/2003)</h>
<ul>
<li><mod>sisyphus</mod> now uses and requires
Python 2.3.</li>
<li>The logging methods can now log everything. If the
logged object is not a string, <mod>pprint</mod> is
used for formatting.</li>
<li>The number of seconds is now properly formatted with
hours, minutes and seconds in the logfiles.</li>
<li>A few methods have been lowercased.</li>
<li>When a job fails the method <meth>failed</meth> is
called now. This gives the job the change to clean up.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.7 (released 03/11/2003)</h>
<ul>
<li><mod>sisyphus</mod> now uses the <mod>ll.url</mod>
module, <mod>ll.fileutils</mod> is no longer required.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.6.2 (released 12/03/2002)</h>
<ul>
<li>error reports are now logged to the process log too.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.6.1 (released 09/10/2002)</h>
<ul>
<li>The <class>Job</class> constructor has a new argument
<arg>printKills</arg> which specifies whether killing a previous
job should be printed (i.e. mailed from cron).</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.6 (released 08/27/2002)</h>
<ul>
<li><mod>sisyphus</mod> has been moved to the
<mod>ll</mod> package.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.5.3 (released 05/07/2002)</h>
<ul>
<li>Derive <class>Job</class> from <class>object</class>
to be able to use new style classes in mixins in subclasses.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.5.2 (released 07/19/2001)</h>
<ul>
<li>Made compatible with fileutils 0.2.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.5.1 (released 04/12/2001)</h>
<ul>
<li>Fixed a severe bug (missing call to <func>os.path.expanduser</func>),
that prevented <class>Job</class> from working.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.5 (released 03/29/2001)</h>
<ul>
<li>The <class>Job</class> constructor has a new parameter
<arg>raiseErrors</arg>. When set to true exceptions
will not only be written to the logfile but raised, which results
in a output to the terminal and an email from the cron daemon.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.4 (released 03/26/2001)</h>
<ul>
<li>The class <class>LogFile</class> has been moved to a seperate module
named <mod>fileutils</mod>.</li>
</ul>
</section>


<section><h>Changes in ll-sisyphus 0.3 (released 02/16/2001)</h>
<ul>
<li>Initial public release</li>
</ul>
</section>

</section>


<section><h>Changes in ll-url</h>

<section><h>Changes in ll-url 0.15.1 (released 03/22/2005)</h>
<ul>
<li>Added a note about the package init file to the installation
documentation.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.15 (released 02/24/2005)</h>
<ul>
<li>The <lit>mimetype</lit> property of <class>ReadResource</class>
is no longer a tuple, but a plain string.</li>
<li><class>ReadResource</class> has a new property named <lit>encoding</lit>,
which is the character encoding of the resource.</li>
<li>A bug in the <lit>lastmodified</lit> property of
<class>WriteResource</class> has been fixed.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.14.2 (released 02/22/2005)</h>
<ul>
<li><lit>url.URL("file:foo/").local()</lit> will now always end in a
directory separator. This didn't work on Windows before.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.14.1 (released 01/13/2005)</h>
<ul>
<li>On Windows <lit>url.File("c:\\foo").abs()</lit> generated
<lit>URL('file:///C|/foo')</lit>. Now the result will always be
<lit>URL('file:/C|/foo')</lit>. The same fix has been made
for <meth>real</meth> and the constructor.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.14 (released 01/03/2005)</h>
<ul>
<li><mod>url</mod> requires the core module and Python 2.4 now.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.13 (released 11/25/2004)</h>
<ul>
<li>The helper function <func>_unescape</func> will now
interpret <lit>%u</lit> escapes (produced by Microsoft software). The
patch has been contributed by Artiom Morozov.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.12.1 (released 11/03/2004)</h>
<ul>
<li>Fixed a bug in the C helper function <func>_unescape</func>
(forget to clear the exception).</li>
<li>Dropped the system default encoding from the list of encodings
that will be tried when UTF-8 fails in <func>_unescape</func>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.12 (released 01/12/2004)</h>
<ul>
<li><func>removefromimportcache</func> has been dropped, now
you can assign the import cache directly (as the module level attribute
<lit>importcache</lit>. Removing modules from the import cache can now be done
via <lit>url.importcache.remove(<rep>mod</rep>)</lit>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11.7 (released 12/23/2003)</h>
<ul>
<li>Fixed a bug in <meth>Path.real</meth> that only surfaced
on Windows.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11.6 (released 12/06/2003)</h>
<ul>
<li>Added a function <func>removefromimportcache</func>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11.5 (released 11/22/2003)</h>
<ul>
<li>Fixed a bug with the <arg>scheme</arg> argument of the methods
<meth>real</meth> and <meth>abs</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11.4 (released 11/19/2003)</h>
<ul>
<li><prop>realurl</prop> has been renamed to <prop>finalurl</prop>
and now works for local &url;s too (it will be the same as the original &url;).</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11.3 (released 11/17/2003)</h>
<ul>
<li>Added an attribute <prop>realurl</prop> to <class>ReadResource</class>
which contains the real &url; (which might be different from the &url; passed to
the constructor, because of a redirect).</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11.2 (released 11/17/2003)</h>
<ul>
<li>&url;s that have an authority part but a relative path will
be properly formatted, i.e. the leading <lit>/</lit> will be included.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11.1 (released 08/13/2003)</h>
<ul>
<li>The <class>URL</class> method <meth>rename</meth> has been fixed.</li>
<li>A bug has been fixed that created relative paths for &http; &url;s that
didn't have a trailing <lit>/</lit>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.11 (released 08/04/2003)</h>
<ul>
<li>A method <meth>withoutfrag</meth> has been added.
<meth>withFragment</meth> has been renamed to <meth>withfrag</meth>
and the property <prop>fragment</prop> has been renamed to
<prop>frag</prop>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.10 (released 07/31/2003)</h>
<ul>
<li><mod>url</mod> requires Python 2.3 now.</li>
<li>The method <mod>insert</mod> has been fixed.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.9.1 (released 07/17/2003)</h>
<ul>
<li>Fixed a bug that drops the filename in <meth>relative</meth>
when both &url;s have the same filenames but a different query.</li>
<li>The fragment is now properly escaped when the &url; is regenerated.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.9 (released 07/09/2003)</h>
<ul>
<li><meth>withExt</meth> and friends have been lowercased.</li>
<li>The <prop>path</prop> has been changed from a string
to an object of the new class <class>Path</class>. This new
class provides many of the path related functionality of &url;s.</li>
<li><p>The method <meth>URL.import_</meth> no longer uses the
import machinery (from the <mod>imp</mod> module), but <func>execfile</func>.
This has the following consequences:</p>
<ul>
<li>You can only import files with the extension <lit>.py</lit>.</li>
<li>The imported module no longer retains deleted attributes of the
previous version.</li>
<li>The file will be compiled even if a bytecode file exists.</li>
</ul>
</li>
</ul>
</section>


<section><h>Changes in ll-url 0.8 (released 06/04/2003)</h>
<ul>
<li>Added methods <meth>abs</meth> and <meth>__rdiv__</meth> to <class>URL</class>.</li>
<li>The method <meth>real</meth> now has an argument <arg>scheme</arg> that
specifies which scheme should the use for the resulting &url;.</li>
<li>Now the query part of an <class>URL</class> will be parsed into the
attribute <prop>query_parts</prop> (which is a dictionary). If the query can't
be parsed, <prop>query_parts</prop> will be <lit>False</lit>, but
<prop>query</prop> will still contain the complete query part.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.7.1 (released 05/01/2003)</h>
<ul>
<li>Made <meth>clearimportcache</meth> a class method.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.7 (released 04/23/2003)</h>
<ul>
<li>Introduced <meth>local</meth> as a synonym for
<meth>asFilename</meth>, <func>Dir</func>
as a synonym for <func>Dirname</func> and
<func>File</func> as a synonym for <func>Filename</func>.</li>
<li>Added functions <func>first</func>, <func>firstdir</func>
and <func>firstfile</func>, that returns the first &url; from
a list that exists, is a directory or a file.</li>
<li>The method <meth>import_</meth> uses a cache now. Different
caching strategies can be chosen through the <arg>mode</arg>
parameter.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.6.2 (released 03/07/2002)</h>
<ul>
<li>The method <meth>real</meth> checked whether the
referenced file really is a directory. This has the problem
that the directory/file must exist. Now the directoryness
of the &url; itself is used.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.6.1 (released 03/06/2002)</h>
<ul>
<li>Fixed a bug in <meth>chown</meth>: Attributes are
not available for <func>pwd.getpwnam()</func> and
<func>grp.getgrnam()</func> results under Python 2.2.
Use the tuple entry instead.</li>
<li>Added methods <meth>mtime</meth>, <meth>atime</meth>
and <meth>size</meth> to <class>URL</class>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.6 (released 03/05/2002)</h>
<ul>
<li>Now all arguments for <meth>walk</meth> default to
<lit>False</lit>.</li>
<li>Added new convenience methods <meth>walkfiles</meth>
and <meth>walkdirs</meth>.</li>
<li>An <class>URL</class> can now be iterated. This is equivalent
to <lit>walk(dirsbefore=True, files=True)</lit>.</li>
<li>Many functions from <mod>os</mod> and <mod>os.path</mod>
have been added as methods to <mod>ll.url</mod>. This was inspired
by Jason Orendorff's
<a href="http://www.jorendorff.com/articles/python/path/"><mod>path</mod></a>
module.</li>
<li>The method <meth>import_</meth> is now available
in the <class>URL</class> class too.</li>
<li>When Python 2.3 is used timestamp will now be <class>datetime.datetime</class>
objects and <mod>mx.DateTime</mod> is no longer required.
With Python 2.2 <mod>mx.DateTime</mod> will still be used.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.5.1 (released 01/07/2002)</h>
<ul>
<li>Added a <filename>LICENSE</filename> file.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.5 (released 11/14/2002)</h>
<ul>
<li><class>WriteResource</class> has been largely rewritten
to elminate the overhead of calls the <meth>write</meth>.
Access to properties might be a little slower now, because
<class>WriteResource</class> has been optimized for maximum
writing speed.</li>
<li>Added source code encoding statements to the Python
files.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.4.3 (released 11/11/2002)</h>
<ul>
<li>Fixed a refcounting leak in the new version of
<func>_normalizepath</func>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.4.2 (released 11/08/2002)</h>
<ul>
<li><func>_normalizepath</func> has been reimplemented
in C for performance reasons.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.4.1 (released 10/29/2002)</h>
<ul>
<li><class>ReadResource</class> and <class>WriteResource</class>
now have a method <meth>import_</meth>, that imports
the file as a Python module (ignoring the file extension).</li>
</ul>
</section>


<section><h>Changes in ll-url 0.4 (released 10/18/2002)</h>
<ul>
<li>Added a HOWTO file.</li>
<li>Made the docstrings compatible with &xist; 2.0.</li>
<li>The <lit>imagesize</lit> property now raises an
<class>IOError</class> if the PIL is not available.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.3.1 (released 09/09/2002)</h>
<ul>
<li><class>WriteResource</class> will now generate an empty
file, even if <meth>write</meth> is never called. This is
checked in <meth>close</meth>.</li>
<li><class>WriteResource</class> gained a destructor that
will call <meth>close</meth>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.3 (released 08/27/2002)</h>
<ul>
<li><mod>url</mod> has been moved to the <mod>ll</mod>
package.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.2 (released 06/18/2002)</h>
<ul>
<li><func>_escape</func> now always uses unicode strings. 8bit
strings will be converted to unicode before the UTF-8 version
will be encoded.</li>
<li><func>_unescape</func> now always emits unicode strings.
If the UTF-8 decoding does not work, the system default encoding will
be tried, and finally Latin-1 will be used.</li>
<li><func>_escape</func> and <func>_unescape</func>
have been rewritten in C for performance reasons.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.8 (released 05/07/2002)</h>
<ul>
<li>Illegal <lit>%</lit> escapes now only issue a warning and will
be used literally when the warning framework doesn't raise an exception.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.7 (released 04/30/2002)</h>
<ul>
<li>Removed the illegal scheme handling change from 0.1.6 again.
Now this has to be done before constructing an <class>URL</class>.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.6 (released 04/26/2002)</h>
<ul>
<li>Now when the parser discovers an illegal scheme, you get
another chance: Beginning whitespace will be stripped and
it will be retried.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.5 (released 04/25/2002)</h>
<ul>
<li>Fixed a bug in <meth>__div__</meth>:
Now <lit>URL("http://foo/bar")/"/baz"</lit> works.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.4 (released 04/15/2002)</h>
<ul>
<li>When assigning to the <prop>url</prop> property, the scheme will
now only be set when it consists of legal characters. This means that parsing
<lit>/foo.php?x=http://www.bar.com</lit> won't try to set a scheme
<lit>/foo.php?x=http</lit>, but will use an empty scheme.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.3 (released 04/09/2002)</h>
<ul>
<li>Make <prop>ext</prop> and <prop>file</prop> work with opaque <class>URL</class>s.</li>
<li>Forgot the make <prop>resdata</prop> assignable. Fixed.</li>
<li>Now the scheme to be used can be specified for the various filename functions.</li>
<li>Added a method <meth>withFragment</meth> that returns a copy of the <class>URL</class>
with a new fragment.</li>
<li>Use the <mod>email</mod> package instead of <mod>rfc822</mod>
for <func>formatdate</func>.</li>
<li>No longer quote <lit>[</lit> and <lit>]</lit> to be compatible with
the ezt templates from <app moreinfo="http://viewcvs.sf.net/">ViewCVS</app>.</li>
<li><p>When joining &url;s the right hand &url; no longer inherits the scheme, if it
has not scheme, but the path is absolute, i.e.</p>
<prog>
&gt;&gt;&gt; url.URL("root:foo.html")/url.URL("/cgi-bin/")
URL('/cgi-bin/')
</prog>
</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.2 (released 03/26/2002)</h>
<ul>
<li>Fixed a bug in <meth>URL.__eq__</meth> and <meth>URL.__hash__</meth>:
<lit>query</lit> and <lit>fragment</lit> were not used. This has been fixed.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1.1 (released 03/20/2002)</h>
<ul>
<li>Fixed a bug in <prop>ReadResource.contentlength</prop>,
which tried to convert the <func>stat</func> result to a
<class>DateTime</class> object.</li>
</ul>
</section>


<section><h>Changes in ll-url 0.1 (released 03/18/2002)</h>
<ul>
<li>Initial release</li>
</ul>
</section>

</section>


<section><h>Changes in ll-xpit</h>

<section><h>Changes in ll-xpit 0.2.1 (released 03/22/2005)</h>
<ul>
<li>Added a note about the package init file to the installation
documentation.</li>
</ul>
</section>


<section><h>Changes in ll-xpit 0.2 (released 01/21/2005)</h>
<ul>
<li><func>convert</func> now takes both a global and a local
namespace and will raise an exception when an unknown processing
instruction target is encountered.</li>
</ul>
</section>


<section><h>Changes in ll-xpit 0.1 (released 01/19/2005)</h>
<ul>
<li>Initial release.</li>
</ul>
</section>

</section>
