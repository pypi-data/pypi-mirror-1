<section><title>Changes in 1.9.1 (released 04/03/2007)</title>

<ulist>
<item>Fixed a bug in <class>ll.url.SshConnection</class>, which was missing
a call to <function>urllib.url2pathname</function>.</item>
</ulist>

</section>


<section><title>Changes in 1.9 (released 03/30/2007)</title>

<ulist>
<item><class>ll.url.Context</class> no longer relies on automatic cleanup for
closing connections. Instead when a <class>Context</class> object is used in a
<lit>with</lit> block, all connections will be closed at the end of the block.
This should finally fix the problem with hanging threads at the end of the program.</item>
<item><par>A script <filename>ucp.py</filename> has been added that can be used to
copy stuff around:</par>
<tty>
ucp -v http://www.python.org ssh://root@www.example.net/~joe/public_html/index.html -u joe -g users
</tty></item>
</ulist>

</section>


<section><title>Changes in 1.8 (released 03/12/2007)</title>

<ulist>
<item>
<par>In calls to <class>ll.url.URL</class> methods that get forwarded
to a connection it's now possible to pass keyword arguments for the connection
constructor directly to the called method, i.e. you can do:</par>
<tty>
<prompt>&gt;&gt;&gt; </prompt><input>u = url.URL("ssh://root@www.example.com/etc/passwd")</input>
<prompt>&gt;&gt;&gt; </prompt><input>u.size(identity="/root/.ssh/id_rsa")</input>
1550
</tty>
</item>
</ulist>

</section>


<section><title>Changes in 1.7.5 (released 03/09/2007)</title>

<ulist>
<item><class>ll.url.Resource</class> now has a method <method>encoding</method>
that returns <lit>None</lit> (for <z>encoding unknown</z>).</item>
</ulist>

</section>


<section><title>Changes in 1.7.4 (released 03/08/2007)</title>

<ulist>
<item><class>ll.url.SshConnection</class>s now supports the <arg>identity</arg>
parameter. This can be used to specify the filename to be used as the identity
file (private key) for authentication.</item>
</ulist>

</section>


<section><title>Changes in 1.7.3 (released 02/22/2007)</title>

<ulist>
<item><class>ll.url.SshConnection</class> now has a new method
<method>close</method> which can be used to shut down the communication channel.
As a <class>SshConnection</class> no longer stores a reference to the context,
this means that ssh connections are shut down immediately after the end of the
context in which they are stored. This avoids a problem with hanging threads.</item>
</ulist>

</section>


<section><title>Changes in 1.7.2 (released 02/02/2007)</title>

<ulist>
<item>Fixed a bug in <function>ll.url._import</function>.</item>
</ulist>

</section>


<section><title>Changes in 1.7.1 (released 01/24/2007)</title>

<ulist>
<item><module>ll.astyle</module> has been updated to the current trunk
version of <link href="http://ipython.scipy.org/">IPython</link>.</item>
<item>As the <module>new</module> module is deprecated, use
<module>types</module> instead.</item>
</ulist>

</section>


<section><title>Changes in 1.7 (released 11/23/2006)</title>

<ulist>
<item>Fixed a bug in the user switching in <class>ll.daemon.Daemon</class>.</item>
<item>Added a new action class <class>GetAttrAction</class> to
<module>ll.make</module>. This action gets an attribute of its input
object.</item>
</ulist>

</section>


<section><title>Changes in 1.6.1 (released 11/22/2006)</title>

<ulist>
<item><class>ll.make.ModuleAction</class> now puts a real filename into the
modules <lit>__file__</lit> attribute, so that source code can be displayed
in stacktraces.</item>
<item><module>ll.astyle</module> has been fixed to work with the current trunk
version of <link href="http://ipython.scipy.org/">IPython</link>.</item>
</ulist>

</section>


<section><title>Changes in 1.6 (released 11/08/2006)</title>

<ulist>
<item><module>ll.url</module> now supports ssh &url;s which are files on remote
hosts. This requires <link href="http://codespeak.net/py/current/doc/execnet.html">py.execnet</link>.
Most of the file data and metadata handling has been rewritten to support the
requirements of ssh &url;s.</item>

<item><class>ll.make.ModeAction</class> and <class>ll.make.OwnerAction</class>
are subclasses of <class>ll.make.ExternalAction</class> now. This means they
will execute even in <z>infoonly</z> mode.</item>

<item>Fixed a bug in <method>ll.make.JoinAction.get</method>.</item>

<item>Remove the pid file for <method>ll.sisyphus.Job</method> when a
<class>KeyboardInterrupt</class> happens and we're running on Python 2.5.</item>

<item>Fixed a longstanding bug in <method>ll.sisyphus.Job</method> which resulted
in the pid file not being written in certain situations.</item>

<item><class>ll.daemon.Daemon</class> now allows to switch the group too.</item>
</ulist>

</section>


<section><title>Changes in 1.5 (released 09/24/2006)</title>

<ulist>
<item><class>ll.make.XISTTextAction</class> is compatible to &xist; 2.15 now.</item>

<item>The functions <function>ll.url.Dirname</function> and
<function>ll.url.Filename</function> have been removed (use
<function>ll.url.Dir</function> and <function>ll.url.File</function>
instead).</item>

<item>The methods <method>ll.url.URL.isLocal</method> and
<method>ll.url.URL.asFilename</method> have been removed (use
<method>ll.url.URL.islocal</method> and <method>ll.url.URL.local</method>
instead).</item>
</ulist>

</section>


<section><title>Changes in 1.4 (released 08/23/2006)</title>

<ulist>
<item>A new module has been added: <module>ll.daemon</module> can be used on UNIX
to fork a daemon running.</item>
</ulist>

</section>


<section><title>Changes in 1.3.2 (released 07/25/2006)</title>

<ulist>
<item><class>ll.make.ModuleAction</class> now normalizes line feeds, so that this
action can now be used directly on Windows too.</item>
</ulist>

</section>


<section><title>Changes in 1.3.1 (released 07/06/2006)</title>

<ulist>
<item>An option <option>showinfoonly</option> has been added to
<class>ll.make.Project</class> (defaulting to <lit>False</lit>). This option
determines whether actions that run in <lit>infoonly</lit> mode are reported or
not.</item>
</ulist>

</section>


<section><title>Changes in 1.3 (released 06/28/2006)</title>

<ulist>
<item><module>ll.make</module> has been rewritten. Now there's no longer a
distinction between <class>Target</class>s and <class>Action</class>s. Actions
can be chained more easily and creating an action and registering it with the
project are two separate steps. Actions can no longer be shared, as each action
stores its own input actions (but output actions are not stored). <z>Ids</z>
have been renamed to <z>keys</z> (and
<class>DBID</class>/<class>OracleID</class> to
<class>DBKey</class>/<class>OracleKey</class>). <class>ImportAction</class> has
been renamed to <class>ModuleAction</class> and can now turn any string into a
module.</item>

<item>In <module>ll.url</module> modification dates for local files now include
microseconds (if the OS supports it).</item>

<item>A class <class>Queue</class> has been added to <module>ll.misc</module>
which provides FIFO queues.</item>

<item>A decorator <function>withdoc</function> has been added to
<module>ll.misc</module> that sets the docstring on the function it
decorates.</item>

<item><module>setuptools</module> is now supported for installation.</item>
</ulist>

</section>


<section><title>Changes in 1.2 (released 12/13/2005)</title>

<ulist>
<item><lit>None</lit> is now allowed as a proper data object in
<module>ll.make</module> actions.</item>

<item><module>ll.xpit</module> now
supports conditionals (i.e. the new processing instruction targets
<lit>if</lit>, <lit>elif</lit>, <lit>else</lit> and <lit>endif</lit>. Now there
<em>must</em> be a space after the target name.</item>
</ulist>

</section>


<section><title>Changes in 1.1.1 (released 11/15/2005)</title>

<ulist>
<item>Fixed a bug in <method>ll.make.Project.buildwithargs</method>.</item>
</ulist>

</section>


<section><title>Changes in 1.1 (released 10/31/2005)</title>

<ulist>
<item><class>ll.make.TOXICAction</class> no longer takes an <arg>encoding</arg>
argument in the constructor, but works on unicode strings directly.</item>

<item>Two new actions (<class>DecodeAction</class> and
<class>EncodeAction</class>) have been added to <module>ll.make</module>.</item>
</ulist>

</section>


<section><title>Changes in 1.0.2 (released 10/24/2005)</title>

<ulist>
<item>Fixed a bug in <method>ll.make.Project.destroy</method> that broke the
<method>recreate</method> method.</item>
</ulist>

</section>


<section><title>Changes in 1.0.1 (released 10/18/2005)</title>

<ulist>
<item>Fixed a bug in <method>ll.make.Project.__contains__.</method></item>
</ulist>

</section>


<section><title>Changes in 1.0 (released 10/13/2005)</title>

<ulist>
<item>This package now contains the following modules, that have been distributed
as separate packages previously: <module>ansistyle</module>,
<module>color</module>, <module>make</module>, <module>misc</module> (which
contains the stuff from the old <module>ll</module> package),
<module>sisyphus</module>, <module>url</module> and <module>xpit</module>.</item>

<item><class>ll.misc.Iterator</class> now has a method <method>get</method> that
will return a default value when the iterator doesn't have the appropriate
item.</item>

<item>In <module>ll.make</module> the output has been fixed: The
<lit>showactionfull</lit> flag is checked before the <lit>showaction</lit> flag
and target id's will always be output in this mode.</item>
</ulist>

</section>
