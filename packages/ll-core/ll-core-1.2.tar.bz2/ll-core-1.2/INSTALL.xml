<?xml version='1.0' encoding='iso-8859-1'?>
<section><title>Requirements</title>
<par>To use the <module>ll.misc</module> package you need the following software package:</par>
<olist>
<item><link href="http://www.python.org/">Python 2.4</link>;</item>
<item>If you want to install from source, a C compiler supported by <app>distutils</app>;</item>
<item>If you want to use the &xist; classes for transforming &xml; with
<module>ll.make</module>:
<pyref module="ll.xist">&xist;</pyref> (version 2.13 or later);</item>
<item>if you want to use <class>TOXICAction</class> or
<class>TOXICPrettifyAction</class> for generating Oracle functions with
<module>ll.make</module>:
<pyref module="ll.toxic">ll-toxic</pyref> (version 0.7 or later);</item>
<item>if you want to use <class>FOPAction</class> for generating &pdf; from
XSL-FO with <module>ll.make</module>:
<link href="http://xml.apache.org/fop/index.html">Apache FOP</link>.</item>
<item>if you want to run the test suite: <link href="http://codespeak.net/py/current/doc/test.html">py.test</link>.</item>
</olist>
</section>

<section><title>Installation</title>
<par><app>distutils</app> is used for installation, so it's rather simple.
Execute the following command:</par>
<prog>
python setup.py install
</prog>
<par>This will compile the C sources and copy the required files to the
<dirname>site-packages</dirname> directory as part of the
<module>ll</module> package.</par>

<par>For Windows a binary distribution is provided. To install it,
double click it and follow the instructions.</par>

<par>If you have difficulties installing this software, send a problem report
to Walter Dörwald (<email>walter@livinglogic.de</email>).</par>
</section>
