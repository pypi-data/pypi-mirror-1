# ========================================================================
# djangoserve configuration file
# ========================================================================

# sys.path preparation
SYSPATH         = None		# if not None, set sys.path to this value
SYSPATH_PREPEND = None		# if not None, prepend to sys.path
SYSPATH_APPEND  = None		# if not None, append to sys.path

# Django settings
DJANGO_SETTINGS = 'vpndashboard.server.settings'
DJANGO_SERVE_ADMIN = True # Serve admin files

# Server settings
IP_ADDRESS = '127.0.0.1'
PORT = 8088
SERVER_NAME = 'localhost'
SERVER_THREADS = 10

# Change it to True if you want it to run as daemon, if you use a
# daemon.sh file you should also change it to True
RUN_AS_DAEMON = False
DAEMON_RUN_DIR = '/' # The daemon will change directory to this one
                     # this is needed to prevent unmounting your
                     # disk.

# Log settings
#
# LOGLEVEL can be one of
#    CRITICAL
#    ERROR
#    WARNING
#    INFO
#    DEBUG
#    NOTSET
LOGFILE  = '/tmp/djangoserve.log'
LOGLEVEL = 'INFO' # if DEBUG is True, overwritten to DEBUG
DEBUG    = True

# Verbosity
#  QUIET   = 0
#  NORMAL  = 1
#  VERBOSE = 2
#  DEBUG   = 3
VERBOSITY = 3

# It must match with the path given in your daemon.sh file if you are
# using a daemon.sh file to control the server. 
PIDFILE = '/var/run/djangoserve.pid' 

# Launch as root to dynamically chown
SERVER_USER  = 'nobody'
SERVER_GROUP = 'nobody'

# Enable SSL, if enabled, the certificate and private key must 
# be provided.
SSL             = False
SSL_CERTIFICATE = '/full/path/to/certificate'
SSL_PRIVATE_KEY = '/full/path/to/private_key'

# Perform internal app server testing
TEST = False

# ========================================================================
