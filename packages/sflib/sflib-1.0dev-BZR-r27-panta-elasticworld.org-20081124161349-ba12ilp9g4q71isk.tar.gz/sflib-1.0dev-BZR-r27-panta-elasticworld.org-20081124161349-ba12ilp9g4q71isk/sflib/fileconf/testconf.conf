#
# this is a test file
#

directive

( (1, 2, 3), 4 )

test = ( ( 1 ) ( "ciao", "pippo" ) )

serverprefix = "www"

server = "foobar"
server.name = "test"
server.url  = "${serverprefix}.test.it"
server.port = 80
server.fancyname = "* ${server.name}:${server.port} *"
server.datecmd = "date"
server.dateopts = "+\%F"
server.date = `${server.datecmd} ${server.dateopts}`
server.desc = "server on ${ENV.HOSTNAME}"
server.home = "${ENV.HOME}"

include "included.conf"

server.port += 1
server.name += " 2"
test += ( 5 )
