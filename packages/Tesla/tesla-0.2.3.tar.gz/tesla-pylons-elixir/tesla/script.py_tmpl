from paste.deploy import loadapp

def run(filename):
    """
    Write your commands here.
    """    
    app = loadapp('config:' + filename)
    from ${package} import models as model
    model.connect()
