
[haus]
app.stack = string(default="['logger', 'standard',  'prefixer', 'static', 'selector']")
load.order = string_list(default=list('locations', 'standard', 'status', 'prefixer', 'logger', 'static', 'selector', 'yaro', 'genshi', 'sessions', 'cache', 'barrel'))

[haus.stacks]
fullstack = string(default="['cache', 'sessions', 'genshi', 'yaro']")
secure = string(default="['cache', 'sessions', 'barrel', 'genshi', 'yaro']")

[selector]
map = string(default="urls.map")

[logging]
level = string(default="DEBUG")

[static]
root = string(default="static")

[app]
prefix = string()

[cache]
data_dir = string(default='.cache')

[sessions]
data_dir = string(default='.sessions')

