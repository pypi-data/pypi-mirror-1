PDIS XPath requires ElementTree or cElementTree to be installed.

setup.py is configured to automatically download and install the latest
ElementTree, but *only* if you do not have any ElementTree installed.
