LoadModule wsgi_module modules/mod_wsgi.so

# needed for imdbpy
WSGIRestrictStdin Off

WSGIScriptAlias /imdbpykit /path/to/imdbpykit.wsgi

Alias /static /path/to/imdbpykit/web/static

<Directory /path/to/imdbpykit>
    Order allow,deny
    Allow from all
</Directory>
