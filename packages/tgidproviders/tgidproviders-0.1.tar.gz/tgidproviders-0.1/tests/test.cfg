# You can place test-specific configuration options here (like test db uri, etc)

[global]
sqlalchemy.dburi = "sqlite:///:memory:"
sqlobject.dburi = "sqlite:///:memory:"

#identity.provider='tgidproviders'
#identity.method=['database', 'ldap', 'smb', 'unix_passwd']

visit.on = True
identity.on = True
identity.failure_url = '/identity_failed'

#TODO: Having these seperate is stupid.  The 3rd line should work!
identity.soprovider.encryption_algorithm = None
identity.saprovider.encryption_algorithm = None
identity.encryption_algorithm = None
identity.custom_encryption = None

# Set up SMB authentication
identity.smb.domain="MYDOMAIN"

# Set up LDAP authentication
identity.ldap.host="localhost"
identity.ldap.port=389
identity.ldap.basedn="dc=localhost"
identity.ldap.filter="(sAMAccountName=%s)"
