"""Defines setup.py commands

Each submodule is a separate command.  For example, a foo command could be run like::
    
    $ python setup.py foo

For information on standard support for setup.py commands, see:
http://docs.python.org/dist/node32.html

"""