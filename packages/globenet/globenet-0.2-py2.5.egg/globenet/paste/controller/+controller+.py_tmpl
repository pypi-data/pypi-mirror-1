#!/usr/bin/env python
#-*- coding: utf-8 -*-
from globenet.web import connect, template

class ${controller.capitalize()}(object):
	@template('controllers/templates/${controller}.html')
	@connect('${controller}','')
	def index(self):
		return {'controller':'${controller.capitalize()}'}

