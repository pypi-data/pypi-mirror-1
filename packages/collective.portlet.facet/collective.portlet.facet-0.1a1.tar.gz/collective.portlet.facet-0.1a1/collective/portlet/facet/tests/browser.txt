Browser tests for collective.portlet.facet
==========================================

FIXME: This is far from complete.

Some setup & login:

    >>> from Products.Five.testbrowser import Browser
    >>> from Products.PloneTestCase.setup import portal_owner, default_password
    >>> browser = Browser()
    >>> browser.handleErrors = False
    >>> portal_url = self.portal.absolute_url()
    >>> portlet_base_url = self.portal.collection.absolute_url()
    >>> browser.open(portal_url)
    >>> browser.getLink('Log in').click()
    >>> browser.getControl(name='__ac_name').value = portal_owner
    >>> browser.getControl(name='__ac_password').value = default_password
    >>> browser.getControl(name='submit').click()
    >>> "You are now logged in" in browser.contents
    True
    
Add the portlet

    >>> browser.open(portlet_base_url + '/++contextportlets++plone.leftcolumn/+/collective.portlet.facet.FacetNavigation')
    >>> browser.getControl(name='form.facets').value = "Subject\nType"
    >>> browser.getControl('Save').click()

    >>> browser.open(portlet_base_url)
    >>> "Facet navigation portlet" in browser.contents
    True
    
    >>> browser.getLink('Manage portlets').click()
    >>> browser.getLink('Facet navigation portlet').click()
    >>> browser.getControl(name='form.portlet_title').value = "Our test portlet"
    >>> browser.getControl('Save').click()

    >>> browser.open(portlet_base_url)
    >>> "Our test portlet" in browser.contents
    True
    