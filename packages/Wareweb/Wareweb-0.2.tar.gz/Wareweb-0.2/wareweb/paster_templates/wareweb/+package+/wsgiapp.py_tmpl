import os
from wareweb import wsgiapp

def make_app(
    global_conf,
    # Required configuration parameters (if any):
    **kw):
    return wsgiapp.make_wareweb_app(
        global_conf,
        package_name=${repr(package)},
        root_path=os.path.dirname(__file__),
        **kw)
