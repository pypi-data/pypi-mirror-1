Simple, very simple. Very similar to createtorrent, slight difference being
that it actually does work.

If you don't know to use distutils, just do this where setup.py lies:

    python setup.py install

and you should be fine.
