## -*- coding: utf-8 -*-
${'==='*len(title)}
:mod:`${title}`
${'==='*len(title)}

.. automodule:: ${title}

.. currentmodule:: ${title}

%if classes:
Classes
=======

%for klass in sorted(classes):
:class:`~${title}.${klass}`
${'--'*len(klass+title)}

.. autoclass:: ${klass} 
   :members:

%endfor
%endif

%if functions:

Functions
=========

%for func in sorted(functions):
:func:`~${title}.${func}`
${'--'*len(func+title)}

.. autofunction:: ${func} 

%endfor
%endif

