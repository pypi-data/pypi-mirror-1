This package, like z3c.viewtemplate, seperates the view code layer from the
template skin layer. Addtionally, it allows an unlimited number of templates
to be plugged into any view class, which makes making masterpages easier.