---------------------------------------
browser method for synchronized content
---------------------------------------


    >>> self.portal
    <PloneSite ...
    >>> from Testing import makerequest
    >>> self.portal = makerequest.makerequest(self.portal)

Install tool::

    >>> self.install_portal_synchronisation()
    True

And test synchronized content::

    >>> self.portal.restrictedTraverse('synchronize_content')
    Traceback (most recent call last):
    ...
    Unauthorized: You are not allowed to access 'synchronize_content' in this context

And now i'am an manager with the good permission::

    >>> self.loginAsPortalOwner()
    >>> self.portal.restrictedTraverse('synchronize_content')
    <Products.Five.metaclass.Synchronize ...
    >>> self.portal.restrictedTraverse('synchronize_content')()

    >>> self.portal.restrictedTraverse('synchronize_deletion')
    <Products.Five.metaclass.SynchroDelete ...
    >>> self.portal.restrictedTraverse('synchronize_deletion')()

