"""
This module defines the WSGI entry point for this application.
"""

from rhubarbtart import TartRootController, make_middleware
from ${package}.webroot import root

def make_app(
    global_conf,
    # Required configuration parameters (if any)...  You can also
    # validate and fill in defaults in configuration here.
    **kw):
    app = TartRootController(root)
    app = make_middleware(app, global_conf, **kw)
    return app
