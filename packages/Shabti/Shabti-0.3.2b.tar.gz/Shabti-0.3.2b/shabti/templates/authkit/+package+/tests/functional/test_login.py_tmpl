from routes import url_for
from {{package}}.tests import *

class TestLoginController(TestController):
    def test_index(self):
        the_url = url(controller='account', action='signin')
        response = self.app.get(the_url)
        # Test response...
        assert 'Using your Passurl' in response
