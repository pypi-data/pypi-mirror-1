from {{package}}.tests import *

class TestAppServer(TestController):
    def test_index(self):
        response = self.app.get('/')
        # Test response...
        print(response)
        assert '<span style="color:lime">AuthKit</span>' in response
