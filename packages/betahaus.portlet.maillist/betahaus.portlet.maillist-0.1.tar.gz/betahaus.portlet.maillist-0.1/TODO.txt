- Portal message should probably be added from it's interface

Optional:
- Add kss for submissions

- Feedback should be sent in the portlet rather than in the main screen

- Use kss instead, so just the portlet is reloaded
