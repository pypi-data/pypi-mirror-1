#!/usr/bin/env python

#from hakmatak.w10n import W10n

from archiwe.constant import APP_NAME,APP_VERSION

from hakmatak.cli.write import Handler

from archiwe.store import WriterClassFactory

if __name__ == '__main__':
    #w10n = W10n()
    #w10n.application = "%s/%s-%s" % (w10n.application,APP_NAME,APP_VERSION)
    handler = Handler(
        storeWriterClassFactory=WriterClassFactory()
        )
    handler.do()
