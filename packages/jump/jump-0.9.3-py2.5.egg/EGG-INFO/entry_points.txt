
      # -*- Entry points: -*-
      [console_scripts]
      jump = jump.commands:jump_command

      [jump.commands]
      dist = jump.commands:JumpDistCommand
      war = jump.commands:JumpWarCommand
      