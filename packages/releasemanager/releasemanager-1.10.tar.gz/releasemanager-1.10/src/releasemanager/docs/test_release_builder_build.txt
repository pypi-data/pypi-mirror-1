>>> import os
>>> from releasemanager.tests import TEST_CONF_PATH
>>> from releasemanager.builder import ReleaseBuilder
>>> rb = ReleaseBuilder("release_builder_template", configPath=os.path.join(TEST_CONF_PATH, "builder"))
>>> rb.build(trunk=True, unique="test_release_builder")
True