from ${base_theme}.browser.interfaces import IThemeSpecific as IParentLayer

class IThemeSpecific(IParentLayer):
    """Marker interface that defines a Zope 3 browser layer, using the
       plone.theme machinery to activate it, but descending from the interface
       of the parent theme so registrations will persist."""
