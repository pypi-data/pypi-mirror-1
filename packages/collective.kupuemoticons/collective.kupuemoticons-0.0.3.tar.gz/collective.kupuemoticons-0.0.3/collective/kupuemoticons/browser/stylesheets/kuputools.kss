/*
 * maybe we can add here in the future a KSS Action on:

   iframe.kupu-editor-iframe:load {
       action-client: plone-initKupu;
   }

 * which adds the buttons dynamically on the client site so that we don't have
 * to overwrite kupu_wysiwyg_support.html to add our custom buttons.
 */

/* vim: set ft=css ts=4 sw=4 expandtab : */
