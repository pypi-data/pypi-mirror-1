Unit tests
==========

Running the tests::

  $ cd /path/to/this/buildout
  $ bin/instance test --nowarning -s iw.sitestat.

Tests of 3rd party components support requires the installation of
these components:

  * Products.PloneArticle
  * Products.Collage
