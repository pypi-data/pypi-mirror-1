This tool lets you view any document with a translation, adding those fields
written in the users preferred languages. This allows documents with incomplete
fields be visualized in another language with all its complete fields.
Functionality

    * Special view of translated documents with fields in the preferred user
	language
    * Uses the languages selected in the language cascade
    * Works with most of the content types
    * Can be activated for the content types selected by the administrator
