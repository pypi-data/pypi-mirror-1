Plone 2.5 GenericSetup profile work
-----------------------------------

- (x) archetypes_tool

   * Add to 'toolset.xml'
 
- (/) kupu_library_tool

   * Add to 'toolset.xml'

   o Read / write 'kupu.xml'

- (/) marshaller

   * Add to 'toolset.xml'

   o Read / write 'marshaller.xml'

- (x) mimetypes_registry

   * Add to 'toolset.xml'

   * Read / write 'mimetypes_registry.xml'

- (x) plone_utils

   * Replace with PlonePAS version in 'tooset.xml'

- (/) portal_atct

   * Add to 'toolset.xml'

   * Read / write 'portal_atct.xml'

     o Verify that topic settings are saved as well as properties

- (_) portal_calendar

   o Read / write 'calendar.xml'

- (x) portal_controlpanel

   * Read / write 'portal_controlpanel.xml'

- (/) portal_form_controller

   * Add to 'toolset.xml'

   o Read / write 'portal_form_controller.xml'

- (/) portal_groupdata

   * Replace with PlonePAS version in 'tooset.xml'

   o Read / write 'portal_groupdata.xml'

- (/) portal_groups

   * Replace with PlonePAS version in 'tooset.xml'

   o Read / write 'portal_groups.xml'

- (/) portal_memberdata

   * Replace with PlonePAS version in 'tooset.xml'

   o Read / write 'portal_memberdata.xml'

- (/) portal_membership

   * Replace with PlonePAS version in 'tooset.xml'

   o Read / write 'portal_membership.xml'

- (_) portal_metadata

   o Read / write 'portal_metadata.xml'

- (?) portal_migration

   ? Get rid of it?

- (/) portal_password_reset

   * Add to 'toolset.xml'

   * Read / write 'portal_password_reset.xml'

- (/) portal_placeful_workflow

   * Add to 'toolset.xml'

   * Read / write 'portal_laceful_workflow.xml'

   * Add configlet to 'controlpanel.xml'

- (_) portal_quickinstaller

   o Read / write 'portal_quickinstaller.xml'

- (_) portal_registration

   o Read / write 'portal_registration.xml'

- (_) portal_syndication

   o Read / write 'portal_syndication.xml'

- (x) portal_transforms

   * Add to 'toolset.xml'

   * Read / write 'portal_transforms.xml'

- (_) portal_uidannotation

   o Read / write 'portal_uidannotation.xml'

- (x) reference_catalog

   * Add to 'toolset.xml'

   * Read / write 'reference_catalog.xml'
 
- (x) uid_catalog

   * Add to 'toolset.xml'

   * Read / write 'uid_catalog.xml'

- (_) Control Panel:  group search bug (in PTS)
