import os
from setuptools import setup, find_packages

version = "0.1"

setup(name="topp.build.<<app>>",
      version=version,
      description="TOPP's buildit process for <<app>>",
      url="http://www.openplans.org/",
      license="GPL",
      classifiers=[], # Get strings from http://www.python.org/pypi?%3Aaction=list_classifiers
      keywords='',
      author='The Open Planning Project',
      author_email='info@openplans.org',
      packages=find_packages(exclude=['ez_setup']),
      namespace_packages=['topp', 'topp.build'],
      include_package_data=True,
      zip_safe=False,
      install_requires=[
        # -*- Extra requirements: -*-
        'topp.build.lib'
        ],
      entry_points="""
        # -*- Entry points: -*-
        [console_scripts]
        build-<<app>> = topp.build.<<app>>:main
        """,
      dependency_links=['http://www.openplans.org/projects/opencore/dependencies']
      )
