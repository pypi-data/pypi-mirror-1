os:

* Add support to nodecheck.NotificationServer for filter checking.
* Add doctests
* Add a "change" object that offers what changed, previous state, and current
state
* Make callback a blocking call; we don't want anymore changes to pass us by while we are executing out callback.

ip:

*

dns:

*
