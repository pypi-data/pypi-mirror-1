#!/usr/bin/python

from django.contrib.billing.models import Account, Receipt

for a in Account.objects.all():
	try:
		r = a.receipt_set.filter(issued__isnull = True).latest("date")
		r._refresh()
	except Receipt.DoesNotExist:
		pass
