int and uchar types

Conditions and masked operations/iteration.  (Conditions return bitmasks, and
operations are only applied on items not masked.)

slicing

swizzles

set new data to default

optimize setting default on creation.

individual column/row selection for multidimensional arrays?

More operations? (trig, blur algorithms)

Add more unit tests for bad arguments.

Save data in a VBO?

insert/delete items into/from arrays?
