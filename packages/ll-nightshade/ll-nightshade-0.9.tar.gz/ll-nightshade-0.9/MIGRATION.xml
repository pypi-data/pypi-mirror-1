<?xml version='1.0' encoding='iso-8859-1'?>
<section><title>Migrating to version 0.8</title>

<par>The class <class>withconnection</class> has been renamed to
<class>Connect</class>.</par>

<par>Calling functions and procedures has changed a bit. Replace the following
old code:</par>

<prog>
proc = nightshade.Call(orasql.Procedure("proc"), connectstring=connectstring)

@cherrypy.expose
def foo(arg):
	return proc(arg)
</prog>

<par>with</par>

<prog>
connection = nightshade.Connect(connectstring=connectstring)
proc = nightshade.Call(orasql.Procedure("proc"), connection)

@cherrypy.expose
def foo(arg):
	return proc(arg)
</prog>

</section>
