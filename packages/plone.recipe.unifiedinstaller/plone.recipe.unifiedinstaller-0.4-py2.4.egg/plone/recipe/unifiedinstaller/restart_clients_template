#!%(shell_cmd)s
#
# ZEO clients restart script
#

if [ ! -w %(file_storage)s ]; then
    echo "You lack the rights necessary to run this script; Try sudo $0"
    exit 1
fi

for client in %(clients)s
do
    echo Restarting $client
    %(bin_dir)s/$client restart
    sleep 1
done
