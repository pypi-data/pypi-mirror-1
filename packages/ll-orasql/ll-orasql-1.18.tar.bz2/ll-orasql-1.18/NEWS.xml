<section><h>Changes in 1.18 (released 01/07/2008)</h>
<ul>
<li>Updated the docstrings to &xist; 3.0.</li>
<li>Added ReST versions of the documentation.</li>
</ul>
</section>


<section><h>Changes in 1.17.5 (released 08/09/2007)</h>
<ul>
<li>Fixed a bug in the error handling of wrong arguments when calling
functions or procedures.</li>
</ul>
</section>


<section><h>Changes in 1.17.4 (released 04/30/2007)</h>
<ul>
<li>The threshold for string length for procedure and function arguments has
been reduced to 4000.</li>
</ul>
</section>


<section><h>Changes in 1.17.3 (released 03/08/2007)</h>
<ul>
<li><lit>BLOB</lit> arguments for procedures and functions are always
passed as variables now.</li>
</ul>
</section>


<section><h>Changes in 1.17.2 (released 03/07/2007)</h>
<ul>
<li>Arguments for procedures and functions that are longer that 32000 characters
are passed as variables now (the threshold was 32768 before and didn't work).</li>
</ul>
</section>


<section><h>Changes in 1.17.1 (released 03/02/2007)</h>
<ul>
<li>Fix an inverted logic bug in <meth>Record.fromdata</meth> that surfaced
in unicode mode: <lit>BLOB</lit>s were treated as string and <lit>CLOB</lit>s
as binary data.</li>
</ul>
</section>


<section><h>Changes in 1.17 (released 02/23/2007)</h>
<ul>
<li>The <lit>readlobs</lit> and <lit>unicode</lit> parameters are now honored
when calling procedures and functions via <class>Procedure</class> and
<class>Function</class> objects.</li>
</ul>
</section>


<section><h>Changes in 1.16 (released 02/21/2007)</h>
<ul>
<li>A parameter <arg>unicode</arg> has been added to various constructors
and methods. This parameter can be used to get strings (i.e. <lit>VARCHAR2</lit>
and <lit>CLOB</lit>s) as <class>unicode</class> object instead of
<class>str</class> objects.</li>
</ul>
</section>


<section><h>Changes in 1.15 (released 02/17/2007)</h>
<ul>
<li>Fixed an output bug in <filename>oradiff.py</filename> when running
in full output mode.</li>
<li>A parameter <arg>readlobs</arg> has been added to various constructors
and methods that can be used to get small (or all) <class>LOB</class>s as
strings in cursor fetch calls.</li>
</ul>
</section>


<section><h>Changes in 1.14 (released 02/01/2007)</h>
<ul>
<li>A new method <meth>iterprivileges</meth> has been added to
<class>Connection</class>.</li>
<li>A script <filename>oragrant.py</filename> has been added for copying
privileges.</li>
</ul>
</section>


<section><h>Changes in 1.13 (released 11/06/2006)</h>
<ul>
<li>Two new methods (<meth>itertables</meth> and <meth>iterfks</meth>)
have been added to <class>Connection</class>. They yield all table definitions
or all foreign keys respectively.</li>
<li>A new method <meth>isenabled</meth> has been added to
<class>ForeignKey</class>.</li>
<li>A <meth>__str__</meth> method has been added to <class>Object</class>.</li>
<li>A bug in <filename>oramerge.py</filename> has been fixed: In certain
situations <filename>oramerge.py</filename> used merging actions that were
meant to be used for the preceeding object.</li>
</ul>
</section>


<section><h>Changes in 1.12.2 (released 10/18/2006)</h>
<ul>
<li>Fixed a bug that showed up when an index and a foreign key
of the same name existed.</li>
</ul>
</section>


<section><h>Changes in 1.12.1 (released 09/19/2006)</h>
<ul>
<li>Fixed a bug in <meth>Index.__xattrs__</meth>.</li>
</ul>
</section>


<section><h>Changes in 1.12 (released 09/06/2006)</h>
<ul>
<li><class>Function</class> objects are now callable too. They return
the return value and a <class>Record</class> containing the modified
input parameters.</li>
</ul>
</section>


<section><h>Changes in 1.11.1 (released 08/29/2006)</h>
<ul>
<li>Fixed a bug in <meth>Column.modifyddl</meth>.</li>
</ul>
</section>


<section><h>Changes in 1.11 (released 08/22/2006)</h>
<ul>
<li>The class <class>Column</class> has gained a few new methods:
<meth>datatype</meth>, <meth>default</meth>, <meth>nullable</meth>
and <meth>comment</meth>.</li>

<li>Calling a procedure will now raise a <class>SQLObjectNotFoundError</class>
error, if the procedure doesn't exist.</li>
</ul>
</section>


<section><h>Changes in 1.10 (released 08/11/2006)</h>
<ul>
<li>The classes <class>Proc</class> and <class>LLProc</class> have
been removed. The functionality of <class>Proc</class> has been merged
into <class>ProcedureDefinition</class> (with has been renamed to
<class>Procedure</class>). Information about the procedure arguments
is provided by the <meth>iteraguments</meth> method.</li>

<li>All other subclasses of <class>Definition</class> have been renamed
to remove the <z>Definition</z> for the name to reduce typing. (Methods
have been renamed accordingly too.)</li>

<li><func>oramerge.main</func> and <func>oradiff.main</func>
now accept option arrays as arguments.</li>

<li><filename>oradiff.py</filename> has finally been fixed.</li>
</ul>
</section>


<section><h>Changes in 1.9.4 (released 08/09/2006)</h>
<ul>
<li>Fixed a bug in <filename>oradiff.py</filename>.</li>
</ul>
</section>


<section><h>Changes in 1.9.3 (released 08/08/2006)</h>
<ul>
<li>Fixed a bug in <filename>oramerge.py</filename>.</li>
</ul>
</section>


<section><h>Changes in 1.9.2 (released 08/04/2006)</h>
<ul>
<li>Fixed a bug in <meth>TableDefinition.iterdefinitions</meth>.</li>
</ul>
</section>


<section><h>Changes in 1.9.1 (released 08/02/2006)</h>
<ul>
<li>Fixed a bug in <filename>oracreate.py</filename>.</li>
</ul>
</section>


<section><h>Changes in 1.9 (released 07/24/2006)</h>
<ul>
<li>Dependencies involving <class>MaterializedViewDefinition</class>s and
<class>IndexDefinition</class>s generated by constraints work properly now,
so that iterating all definitions in create order really results in a
working &sql; script.</li>

<li>A method <meth>table</meth> has been added to <class>PKDefinition</class>,
<class>FKDefinition</class>, <class>UniqueDefinition</class> and
<class>IndexDefinition</class>. This method returns the <class>TableDefinition</class>
to object belongs to.</li>

<li>A method <meth>pk</meth> has been added to <class>FKDefinition</class>.
It returns the primary key that this foreign key references.</li>

<li>Indexes and constraints belonging to skipped tables are now skipped too
in <filename>oracreate.py</filename>.</li>

<li>Arguments other than <lit>sys.argv[1:]</lit> can now be passed to the
<filename>oracreate.py</filename> and <filename>oradrop.py</filename>
<func>main</func> functions.</li>

</ul>
</section>


<section><h>Changes in 1.8.1 (released 07/17/2006)</h>
<ul>
<li><mod>ll.orasql</mod> can now handle objects name that
are not in uppercase.</li>
</ul>
</section>


<section><h>Changes in 1.8 (released 07/14/2006)</h>
<ul>
<li><meth>Connection.iterobjects</meth> has been renamed to
<meth>iterdefinitions</meth>.</li>

<li>Each <class>Definition</class> subclass has a new classmethod
<meth>iterdefinitions</meth> that iterates through all definitions
of this type in a schema (or all schemas).</li>

<li>Each <class>Definition</class> subclass has new methods
<meth>iterreferences</meth> and <meth>iterreferencedby</meth> that
iterate through related definitions. The methods <meth>iterreferencesall</meth>
and <meth>iterreferencedbyall</meth> do this recursively. The method
<meth>iterdependent</meth> is gone now.</li>

<li>The method <meth>iterschema</meth> of <class>Connection</class>
now has an additional parameter <arg>schema</arg>. Passing <lit>"all"</lit>
for <arg>schema</arg> will give you statistics for the complete database
not just one schema.</li>

<li>A new definition class <class>MaterializedViewDefinition</class> has
been added that handles materialized views. Handling of create options is
rudimentary though. Patches are welcome.</li>

<li><class>TableDefinition</class> has a three new methods:
<meth>ismview</meth> returns whether the table is a materialized view;
<meth>itercomments</meth> iterates through comments and
<meth>iterconstraints</meth> iterates through primary keys, foreign keys
and unique constraints.</li>

<li>The method <meth>getcursor</meth> will now raise a <class>TypeError</class>
if it can't get a cursor.</li>
</ul>
</section>


<section><h>Changes in 1.7.2 (released 07/05/2006)</h>
<ul>
<li><lit>RAW</lit> fields in tables are now output properly in
<meth>TableDefinition.createddl</meth>.</li>

<li>A class <class>PackageBodyDefinition</class> has been added.
<filename>oracreate.py</filename> will output package body definitions
and <filename>oradrop.py</filename> will drop them.</li>
</ul>
</section>


<section><h>Changes in 1.7.1 (released 07/04/2006)</h>
<ul>
<li>Duplicate code in the scripts has been removed.</li>
<li>Fixed a bug in <filename>oramerge.py</filename>: If the source
to be diffed was long enough the call to <filename>diff3</filename> deadlocked.</li>
</ul>
</section>


<section><h>Changes in 1.7 (released 06/29/2006)</h>
<ul>
<li>The method <meth>iterobjects</meth> has been moved from
<class>Cursor</class> to <class>Connection</class>.</li>
<li>The method <meth>itercolumns</meth> has been moved from
<class>Cursor</class> to <class>TableDefinition</class>.</li>
<li><class>LLProc</class> now recognizes the <lit>c_out</lit>
parameter used by <mod>ll.toxic</mod> 0.8.</li>
<li>Support for positional arguments has been added for <class>Proc</class>
and <class>LLProc</class>. Error messages for calling procedures
have been enhanced.</li>
<li><class>SequenceDefinition</class> now has a new method
<meth>createddlcopy</meth> that returns code that copies the
sequence value. <filename>oracreate.py</filename> has a new
option <option>-s</option>/<option>--seqcopy</option> that
uses this feature.</li>
<li><mod>setuptools</mod> is now supported for installation.</li>
</ul>
</section>


<section><h>Changes in 1.6 (released 04/26/2006)</h>
<ul>
<li>Added a <class>SessionPool</class> (a subclass of <mod>cx_Oracle</mod>s
<class>SessionPool</class>) whose <meth>acquire</meth> method
returns <mod>ll.orasql.Connection</mod> objects.</li>
</ul>
</section>


<section><h>Changes in 1.5 (released 04/05/2006)</h>
<ul>
<li>Added a class <class>IndexDefinition</class> for indexes.
<filename>oracreate.py</filename> will now issue create statements
for indexes.</li>
</ul>
</section>


<section><h>Changes in 1.4.3 (released 12/07/2005)</h>
<ul>
<li>Fixed a bug with empty lines in procedure sources.</li>
<li>Remove spurious spaces at the start of procedure and function
definitions.</li>
</ul>
</section>


<section><h>Changes in 1.4.2 (released 12/07/2005)</h>
<ul>
<li>Fixed a bug that the DDL output of Java source.</li>
<li>Trailing whitespace in each line of procedures, functions etc.
is now stripped.</li>
</ul>
</section>


<section><h>Changes in 1.4.1 (released 12/06/2005)</h>
<ul>
<li>Fixed a bug that resulted in omitted field lengths.</li>
</ul>
</section>


<section><h>Changes in 1.4 (released 12/05/2005)</h>
<ul>
<li>The option <option>-m</option>/<option>--mode</option> has been dropped
from the script <filename>oramerge.py</filename>.</li>
<li>A new class <class>ColumnDefinition</class> has been added to
<mod>ll.orasql</mod>. The <class>Cursor</class> class has a new method
<meth>itercolumns</meth> that iterates the <class>ColumnDefinition</class>s
of a table.</li>
<li><filename>oramerge.py</filename> now doesn't output a merged
<lit>create table</lit> statement, but the appropriate <lit>alter table</lit>
statements.</li>
</ul>
</section>


<section><h>Changes in 1.3 (released 11/24/2005)</h>
<ul>
<li>Added an option <option>-i</option> to <filename>oracreate.py</filename>
and <filename>oradrop.py</filename> to ignore errors.</li>
<li>The argument <arg>all</arg> of the cursor method <meth>iterobjects</meth>
is now named <arg>schema</arg> and may have three values: <lit>"own"</lit>,
<lit>"dep"</lit> and <lit>"all"</lit>.</li>
<li>Added an script <filename>oramerge.py</filename> that does a three
way merge of three database schemas and outputs the resulting script.</li>
<li>DB links are now copied over in <class>SynonymDefinition</class>s.</li>
</ul>
</section>


<section><h>Changes in 1.2 (released 10/24/2005)</h>
<ul>
<li>Added a argument to <meth>createddl</meth> and <meth>dropddl</meth>
to specify if terminated or unterminated DDL is wanted (i.e. add <lit>;</lit> or
<lit>/</lit> or not).</li>

<li><class>CommentsDefinition</class> has been renamed to <class>CommentDefinition</class>
and holds the comment for one field only.</li>

<li><class>JavaSourceDefinition</class> has been added.</li>

<li>The scripts <filename>oracreate.py</filename>, <filename>oradrop.py</filename>
and <filename>oradiff.py</filename> now skip objects with <lit>"$"</lit> in their
name by default. This can be changed with the <option>-k</option> option (but
this will lead to unexecutable scripts).</li>

<li><filename>oradiff.py</filename> has a new options <option>-b</option>:
This allows you to specify how whitespace should be treated.</li>

<li>Added an option <option>-x</option> to <filename>oracreate.py</filename> to
make it possible to directly execute the DDL in another database.</li>

<li>Fixed a bug in <class>SequenceDefinition</class> when the <lit>CACHE</lit>
field was <lit>0</lit>.</li>
</ul>

</section>


<section><h>Changes in 1.1 (released 10/20/2005)</h>
<ul>
<li>A script <filename>oradiff.py</filename> has been added which can be
used for diffing Oracle schemas.</li>

<li>Definition classes now have two new methods <meth>cdate</meth> and
<meth>udate</meth> that give the creation and modification time of the
schema object (if available).</li>

<li>A <lit>"flat"</lit> iteration mode has been added to
<meth>Cursor.iterobjects</meth> that returns objects unordered.</li>

<li><class>Connection</class> has a new method <meth>connectstring</meth>.</li>

<li>A class <class>LibraryDefinition</class> has been added.</li>

<li><meth>CommentsDefinition.createddl</meth> returns <lit>""</lit>
instead of <lit>"\n"</lit> now if there are no comments.</li>

<li><class>SQLObjectNotfoundError</class> has been renamed to
<class>SQLObjectNotFoundError</class>.</li>
</ul>
</section>


<section><h>Changes in 1.0 (released 10/13/2005)</h>
<ul>
<li><mod>ll.orasql</mod> requires version 1.0 of the core package now.</li>

<li>A new generator method <func>iterobjects</func> has been added
to the <class>Cursor</class> class. This generator returns <z>definition objects</z>
for all the objects in a schema in topological order (i.e. if the name of an
object (e.g. a table) is generated it will only depend on objects whose name has
been yielded before). &sql; for recreating and deleting these &sql; objects 
can be generated from the definition objects.</li>

<li>Two scripts (<filename>oracreate.py</filename> and
<filename>oradrop.py</filename>) have been added, that create &sql; scripts
for recreating or deleting the content of an Oracle schema.</li>
</ul>
</section>


<section><h>Changes in 0.7 (released 08/09/2005)</h>
<ul>
<li>The commands generated by <func>iterdrop</func> no longer have
a terminating <lit>;</lit>, as this seems to confuse Oracle/cx_Oracle.</li>
</ul>
</section>


<section><h>Changes in 0.6 (released 06/20/2005)</h>
<ul>
<li>Two new functions have been added: <func>iterdrop</func> is
a generator that yields information about how to clear the schema (i.e. drop
all table, sequences, etc.). <func>itercreate</func> yields information
about how to recreate a schema.</li>
</ul>
</section>


<section><h>Changes in 0.5 (released 06/07/2005)</h>
<ul>
<li>Date values are now supported as <lit>OUT</lit> parameters.</li>
</ul>
</section>


<section><h>Changes in 0.4.1 (released 03/22/2005)</h>
<ul>
<li>Added a note about the package init file to the installation
documentation.</li>
</ul>
</section>


<section><h>Changes in 0.4 (released 01/03/2005)</h>
<ul>
<li><mod>ll.orasql</mod> now requires <app>ll-core</app>.</li>
<li>Procedures can now be called with string arguments longer that 32768
characters. In this case the argument will be converted to a variable before
the call. The procedure argument must be a <lit>CLOB</lit> in this case.</li>
<li>Creating <class>Record</class> instances from database data is now done
by the class method <meth>Record.fromdata</meth>. This means it's now
possible to use any other class as long as it provides this method.</li>
</ul>
</section>


<section><h>Changes in 0.3 (released 12/09/2004)</h>
<ul>
<li><mod>ll.orasql</mod> requires <app>cx_Oracle 4.1</app> now.</li>
</ul>
</section>


<section><h>Changes in 0.2.1 (released 09/09/2004)</h>
<ul>
<li>Fixed a regression bug in <meth>Proc._calcrealargs</meth> as
cursors will now always return <class>Record</class> objects.</li>
</ul>
</section>


<section><h>Changes in 0.2 (released 09/08/2004)</h>
<ul>
<li>Now generating <class>Record</class> object is done automatically
in a subclass of <class>cx_Oracle.Cursor</class>. So now it's possible
to use <mod>ll.orasql</mod> as an extended <mod>cx_Oracle</mod>.</li>
</ul>
</section>


<section><h>Changes in 0.1 (released 07/15/2004)</h>
<ul>
<li>Initial release.</li>
</ul>
</section>
