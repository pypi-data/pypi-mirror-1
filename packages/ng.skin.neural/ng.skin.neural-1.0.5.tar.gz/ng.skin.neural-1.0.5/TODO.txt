$Id: TODO.txt 49764 2008-01-28 18:55:09Z cray $
$URL: https://code.dreambot.ru/svn/ng.skin.neural/tags/ng.skin.neural-1.0.5/TODO.txt $

План работ по neural.ru:

    4.  Придумать как сделать рубрикатор -- типа, как интегрировать с сайтом.
        хотелось бы задействовать оба метода.

    5.  Написать страницу, удалящую из uniqid битые ссылки
    for a,b in ii.items() :
    ...  if 'file2file' in str(ii.getObject(a)) :
    ...    ii.unregister(ii.getObject(a))
    ...
    >>> for a,b in ii.items() :
    ...  if 'file2file' in str(ii.getObject(a)) :
    ...   print a,ii.getObject(a)
        
Конец документа